JWT_SECRET=
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
# Optional: show the "Fill Admin/Officer" demo-login buttons even on a
# production build (they render by default outside production already).
NEXT_PUBLIC_SHOW_DEMO_LOGIN=false
